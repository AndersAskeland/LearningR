library(tidyverse)

# Making code more readable section (Styler) ------------------------------------------------------------

# Object names
DayOne
T <- FALSE
c <- 9

# Spacing
x[, 1]
x[, 1]
mean(x, na.rm = TRUE)
mean(x, na.rm = TRUE)
height <- feet * 12 + inches
df$ z
x <- 1:10


